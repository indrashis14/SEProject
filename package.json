{
  "name": "SEProject",
  "version": "1.0.0",
  "description": "Course project for BPHC",
  "scripts": {
    "start": "concurrently \"npm start --prefix client\" \"npm run start --prefix server\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Shreyas",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "concurrently": "^6.5.1"
  }
}
